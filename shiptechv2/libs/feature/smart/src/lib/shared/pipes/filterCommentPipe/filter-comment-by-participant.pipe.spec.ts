import { FilterCommentByParticipantPipe } from './filter-comment-by-participant.pipe';

describe('FilterCommentByParticipantPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCommentByParticipantPipe();
    expect(pipe).toBeTruthy();
  });
});
