import { EnUsDatePipePipe } from './en-us-date-pipe.pipe';

describe('EnUsDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new EnUsDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
