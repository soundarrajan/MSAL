This directory contains scripts that implement normalizers for server JSON data,
in order to allow smooth transition in case of later changes in the UI.
I.e if using something other than DataTables should be decided further down the road,
this "best practice" approach will allow for a smooth transition, not affecting the
server API data format at all.

See each script for details.