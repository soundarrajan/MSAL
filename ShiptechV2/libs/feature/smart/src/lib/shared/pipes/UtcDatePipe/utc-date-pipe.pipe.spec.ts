import { UtcDatePipePipe } from './utc-date-pipe.pipe';

describe('UtcDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UtcDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
