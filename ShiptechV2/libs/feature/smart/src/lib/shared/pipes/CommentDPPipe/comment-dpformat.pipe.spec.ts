import { CommentDPFormatPipe } from './comment-dpformat.pipe';

describe('CommentDPFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CommentDPFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
