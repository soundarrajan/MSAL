import { GroupByParticipantPipe } from './group-by-participant.pipe';

describe('GroupByParticipantPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByParticipantPipe();
    expect(pipe).toBeTruthy();
  });
});
